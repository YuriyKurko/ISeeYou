<Window x:Class="Server.ShareScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Server"
        mc:Ignorable="d"
        Title="Shared screen"
        Height="400" 
        Width="600"
        MinHeight="400"
        MinWidth="600"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.90*"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>
        <Image x:Name="screenImage" Grid.Row="0"/>
        <Button x:Name="shareScreenButton" Grid.Row="1" Content="Start sharing" HorizontalAlignment="Left" Margin="10,10" VerticalAlignment="Top" Width="120" Height="25" FontSize="14" Click="shareScreenButton_Click" />
    </Grid>
</Window>
